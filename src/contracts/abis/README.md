# Contract ABIs

Paste the deployed-contract ABI JSON arrays into:

- `PurposeToken.json`
- `BountyManager.json`
- `VendorRedemptionManager.json`

`ERC20.json` is the minimal ABI for USDC reads/writes and is already populated.
